import Head from "next/head";
import FooterComponet from "../components/footer";
import HeaderComponent from "../components/header";
import WavesComponent from "../components/waves";
import styles from "../styles/Home.module.scss";
import AboutComponent from "../components/about";
import { useState } from "react";
import InitialActionComponent from "../components/initial_action";

export default function Home() {
  const [current_page, set_current_page] = useState("1");
  const [current_component, set_current_component] = useState(
    <AboutComponent></AboutComponent>
  );

  switch (current_page) {
    case "1":
      return (
        <div className={styles.container}>
          <Head>
            <title>Bem vindo a Orlla!</title>
            <meta name="description" content="Generated by create next app" />
            <link rel="icon" href="/orlla-logo-original.svg" />
          </Head>

          <HeaderComponent set_current_page={set_current_page} />

          <main className={styles.main}>
            <InitialActionComponent></InitialActionComponent>
            <WavesComponent></WavesComponent>
          </main>

          <FooterComponet />
        </div>
      );
    case "2":
      return (
        <div className={styles.container}>
          <Head>
            <title>Bem vindo a Orlla!</title>
            <meta name="description" content="Generated by create next app" />
            <link rel="icon" href="/orlla-logo-original.svg" />
          </Head>

          <HeaderComponent set_current_page={set_current_page} />

          <main className={styles.main}>
            <AboutComponent></AboutComponent>
            <WavesComponent></WavesComponent>
          </main>

          <FooterComponet />
        </div>
      );
  }
}
